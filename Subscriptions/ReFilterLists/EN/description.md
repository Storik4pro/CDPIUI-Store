Re:filter is an attempt to create a relevant list of blocked domains and IPs in Russia, along with popular domains that are also blocked for Russian users.

It allows you to replace the site list of the current preset with a list of all blocked sites.
To replace the site list, visit the component settings page, select the desired site list, open [Additional actions], and select [Replace site list].